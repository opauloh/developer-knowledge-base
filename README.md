This is a compiled knowledge about Javascript / Typescript / React, algorithms, patterns and concepts

### Tips 

Running a local server of built files for local test:

```bash
npx serve -s build
```
